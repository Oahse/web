# ============================================
# BANWEE BACKEND CONFIGURATION
# ============================================
# This file works for both local development and Docker environments
# Copy this file to .env and update the values for your environment

# IMPORTANT: Never commit the actual .env file to version control!
# The .env file should contain your actual secrets and configuration.

# Database Configuration
# ======================

# PostgreSQL database username
# REQUIRED
POSTGRES_USER=banwee

# PostgreSQL database password
# REQUIRED
# ⚠️  SENSITIVE: Keep this value secure!
POSTGRES_PASSWORD=banw*******word

# PostgreSQL server hostname
# REQUIRED
# Environment-specific overrides:
#   Development: localhost
#   Production: postgres
POSTGRES_SERVER=postgres

# PostgreSQL server port
# REQUIRED
# Pattern: ^\d{1,5}$
POSTGRES_PORT=5432

# PostgreSQL database name
# REQUIRED
POSTGRES_DB=banwee_db

# Complete PostgreSQL connection URL
# OPTIONAL
# Pattern: ^postgresql\+asyncpg://.*
POSTGRES_DB_URL=postgresql+asyncpg://user:password@host:5432/database

# Redis connection URL
# REQUIRED
# Environment-specific overrides:
#   Development: redis://localhost:6379/0
#   Production: redis://redis:6379/0
REDIS_URL=redis://redis:6379/0

# Kafka bootstrap servers
# REQUIRED
# Environment-specific overrides:
#   Development: localhost:9092
#   Production: kafka:29092
KAFKA_BOOTSTRAP_SERVERS=kafka:29092

# Security Configuration
# ======================

# Application secret key for JWT and encryption
# REQUIRED
# ⚠️  SENSITIVE: Keep this value secure!
# Pattern: ^.{32,}$
SECRET_KEY=your***********************************************hars

# Stripe API secret key
# REQUIRED
# ⚠️  SENSITIVE: Keep this value secure!
# Pattern: ^sk_(test_|live_)[a-zA-Z0-9]{99}$
STRIPE_SECRET_KEY=sk_t***************************here

# Stripe webhook endpoint secret
# REQUIRED
# ⚠️  SENSITIVE: Keep this value secure!
# Pattern: ^whsec_[a-zA-Z0-9_]{10,}$
STRIPE_WEBHOOK_SECRET=whse*****************************here

# Mailgun API key for email sending
# OPTIONAL
# ⚠️  SENSITIVE: Keep this value secure!
MAILGUN_API_KEY=your*****************here

# External Services Configuration
# ===============================

# Mailgun domain for email sending
# OPTIONAL
MAILGUN_DOMAIN=yourdomain.com

# Application Configuration
# =========================

# Application environment (local, staging, production)
# REQUIRED
# Pattern: ^(local|staging|production)$
ENVIRONMENT=local

# Frontend application URL
# REQUIRED
FRONTEND_URL=http://localhost:5173


# ============================================
# SETUP INSTRUCTIONS
# ============================================

# 1. Copy this file to .env:
#    cp .env.example .env

# 2. Update the values above with your actual configuration

# 3. For local development:
#    - Use localhost for database/redis connections
#    - Use test Stripe keys (sk_test_...)
#    - Generate a secure SECRET_KEY: openssl rand -hex 32

# 4. For production:
#    - Use Docker service names for connections
#    - Use live Stripe keys (sk_live_...)
#    - Use strong, unique passwords and secrets

# 5. Environment-specific behavior:
#    - Set ENVIRONMENT=local for development
#    - Set ENVIRONMENT=production for production
#    - Overrides are applied automatically based on ENVIRONMENT
